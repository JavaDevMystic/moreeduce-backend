spring.application.name=moreeduce

# Database settings
spring.datasource.url=${JDBC_DATABASE_URL:jdbc:postgresql://localhost:5432/moreeduce}
spring.datasource.username=${JDBC_DATABASE_USERNAME:postgres}
spring.datasource.password=${JDBC_DATABASE_PASSWORD:db password}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Token Settings
token.signing.key=${TOKEN_SIGNING_KEY:}
token.access.expiration=${TOKEN_ACCESS_EXPIRATION:86400000}
token.refresh.expiration=${TOKEN_REFRESH_EXPIRATION:259200000}

# Google Cloud Storage Settings
spring.cloud.gcp.core.enabled=false
spring.cloud.gcp.storage.enabled=false
spring.cloud.gcp.storage.auto-create-files=false
gcp.storage.bucket-name=${GCP_STORAGE_BUCKET_NAME:storage-bucket name yoziladi}

# File Upload Limits
spring.servlet.multipart.max-file-size=150MB
spring.servlet.multipart.max-request-size=155MB

# Mail Settings (Brevo API)
spring.mail.username=${MAIL_USERNAME: brevo ochilgan account ning email yoziladi}
spring.mail.password=${MAIL_PASSWORD}

# AI Settings (Google Gemini)
gemini.api.key=${GEMINI_API_KEY}
# Eng yangi va tezkor model: gemini-1.5-flash
gemini.api.url=gemini url 

# Render Deploy Fix
server.forward-headers-strategy=framework

# Exception Handling
spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=false

# Super Admin Default Credentials
app.super-admin.email=${SUPER_ADMIN_EMAIL:superadmin uchun email yoziladi}
app.super-admin.password=${SUPER_ADMIN_PASSWORD:superadmin123}
